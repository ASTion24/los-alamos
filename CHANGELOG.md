# 更新日志

本文件记录 Los Alamos 的用户可见变化。版本遵循
[Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.2.0] - 2026-09-10

### 此刻工作台

- 默认首页改为一个可以直接进入工作的统一入口，而不是项目管理总览。
- 新增挂念收纳箱：保存原话不会自动创建项目、任务或完成度。
- 收纳项可准备、暂放、取回或转换为经过用户确认的项目。
- 首页根据时间、精力、依赖、交接和活动计划选择一个可执行入口。
- 预览不会创建会话；确认开始使用内容 token，任务或边界变化后旧确认自动失效。
- 工作期间可以收下分心事项，不切换项目、不暂停计时、不扩大本轮范围。
- 材料入口支持显式打开 HTTP(S) 链接和常见文档；脚本、目录与未知类型只在文件管理器定位。

### 连续驻留

- 新增结构化 `ContextCapsule`，保存停靠点、决定、材料、阻塞、下一动作和不做范围。
- 结束驻留的默认交接缩减为“停在哪里”和“下次第一动作”，其余既有上下文自动继承。
- 新增暂停、恢复、心跳和异常中断恢复；离开应用的时间不会计入有效工作时间。
- 部分完成和检查点不再根据耗时或模型猜测自动增加任务进度。
- 项目可以明确关闭、暂存、等待外部条件、停止投入或恢复活动状态。
- 新增可选的多日 Residency Plan，约束日期、项目范围、每日容量、强度和退出标准。
- 完成最后任务后直接提供项目收尾入口，关闭需要可核验依据。

### 建模与协议

- 本地分析器优先使用用户确认的剩余动作，不再生成固定的通用五步计划。
- 用户自述起点与叶任务加权进度分离，避免将“我大概完成了 80%”伪装成精确进度。
- 事实变化会将旧模型标记为待校准，并阻止依据过期任务图继续调度。
- 新增收纳、交接和计划 Schema，以及对应 CLI、Agent 能力与确定性状态机指令。
- `focus preview` 与 `focus start` 提供可审阅、可过期、并发安全的短路径。
- 公开写操作增加跨进程工作区锁；转换收纳项和启动会话具备幂等/并发保护。

### 桌面体验

- 重构项目详情、计划、弹层和驻留连续性界面，保留原有任务图与历史能力。
- 首页使用稳定的浅色工作台布局，工作入口与收纳区域在视觉和操作上分离。
- 弹层使用 Portal、背景 inert、焦点陷阱、Escape 关闭和一致的忙碌状态。
- 支持 `1180×760` 与最小窗口 `980×640`；长文本不会造成横向溢出。
- 保留 Electron `sandbox`、`contextIsolation` 和受限 preload API。

### 验证

- 自动测试扩展至 8 个测试文件、80 项用例。
- 新增真实 Electron 的此刻工作台与连续驻留端到端验收。
- CI 在 macOS、Windows 和 Linux 原生包上执行启动、IPC、Agent Runtime 与 GUI 回归。
- 验收步骤和边界记录于 `docs/launcher-acceptance.md` 与
  `docs/continuity-acceptance.md`。

### 兼容性

- `schemaVersion` 保持为 `1`；新增字段均为可选，旧工作区无需迁移即可读取。
- 不会在升级时重建旧任务图或改写历史事件。
- macOS、Windows 和 Linux 安装目标保持不变。

## [0.1.0] - 2026-08-18

### 初始版本

- 提供本地优先的 Electron 桌面应用与任务图建模。
- 支持时间/强度约束下的驻留提案、开始、结束和加权进度。
- 提供 OpenAI-compatible 模型接入与本地规则回退。
- 建立 GUI、CLI、Agent Skill 与文件协议共享的工作区。
- 建立 macOS、Windows 和 Linux 构建及原生 smoke test 流程。

[0.2.0]: https://github.com/ASTion24/los-alamos/releases/tag/v0.2.0
[0.1.0]: https://github.com/ASTion24/los-alamos/commit/3cbab37
